/* from picamera import PiCamera
from time import sleep

camera = PiCamera()

camera.rotation = 180   // optional depending on how camera is rotated
camera.start_preview()
sleep(10)
camera.stop_preview() */